{
  "name": "fable-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Programmatic run/judge/retry orchestration for Claude Code sessions - the control flow prompts cannot enforce.",
  "scripts": {
    "fable": "tsx src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.198"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
